# g0_robot_lab Run Commands

The current run-command reference has moved to:

```text
docs/run_commands.md
```

Keep this file as a compatibility pointer for older notes that linked to `docs/g0_robot_lab_run_commands.md`.

Current project:

```text
g0_robot_lab
```

Current task:

```text
G0-Velocity-v0
```

Current train/play entry points:

```text
scripts/rsl_rl/train.py
scripts/rsl_rl/play.py
```

Do not use old `humanoid_lab_v0` commands or the old local `tasks/manager_based/` template path for the current G0 locomotion baseline.
